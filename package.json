{
  "name": "ticno-code-local",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "esbuild static/js/index.js --bundle --outfile=static/js/main.dev.js --loader:.png=dataurl --loader:.gif=dataurl --loader:.svg=file --asset-names=../media/[name].[hash] --loader:.css=css --loader:.js=jsx --jsx=automatic --define:process.env.NODE_ENV=\"'development'\" --target=es2017 --watch=forever"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
